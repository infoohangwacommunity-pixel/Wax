WAX POST-OMEGA

PRINCIPAL RUNTIME MISSION

From Verified Constitutional Foundation to an Open-World AI Environment

Role: Principal Engineer / Runtime Architect / Research Engineer
Authority: Founder
Repository: WAX
Mission Type: Continuous architectural research, implementation, verification, and reality alignment
Starting Point: WAX OMEGA Final Reality
Operating Principle: Infrastructure, not intelligence.

0. READ THIS FIRST

You are not being asked to build another AI application.

You are not being asked to add a collection of impressive features.

You are not being asked to turn WAX into a better tutor, chatbot, assistant, productivity application, WhatsApp bot, agent framework, or collection of tools.

You are being asked to continue the construction of WAX as a general-purpose runtime environment in which intelligence can pursue legitimate human objectives through durable, observable, secure, composable infrastructure.

The previous OMEGA cycle established and verified a substantial constitutional foundation.

Your job is to take that foundation seriously.

Do not restart the architecture.

Do not throw away working infrastructure because a different architecture sounds cleaner.

Do not blindly implement every item labelled “future,” “deferred,” or “next.”

Do not assume that a previous agent's report is correct merely because it is detailed.

Do not assume that the current repository is correct merely because tests pass.

Do not assume that a missing feature is automatically a bug.

Do not assume that adding more capabilities makes WAX more capable.

Instead:

Interrogate reality. Understand the existing runtime. Identify the next genuine architectural bottlenecks. Research them. Design them constitutionally. Implement only what belongs. Verify everything. Document the boundaries. Then continue.

The repository is the authority on what exists.

The constitution is the authority on what WAX is allowed to become.

Evidence is the authority on whether a claim is true.

The founder is the authority on unresolved philosophical/product decisions.

1. CURRENT FOUNDATION — DO NOT IGNORE IT

Two founder reports accompany this mission:

WAX Constitutional Audit — Founder Report

WAX OMEGA — Final Reality Report

Read both completely before changing code.

The Constitutional Audit reported that five independent review tracks examined the source tree, database tables, capabilities, prompts, state machines, and migrations, and classified findings as LIVE, IMPLEMENTED-BUT-UNWIRED, TEST-ONLY, DOCUMENTATION-ONLY, PLACEHOLDER, DEAD, or CONSTITUTIONAL VIOLATION.

The audit corrected ten constitutional violations.

The subsequent OMEGA cycle continued from that foundation.

The OMEGA final report states that the repository had reached:

655 passing tests

13/13 live probe checks

14 constitutional violations corrected across two audit cycles

0 open violations

approximately 122 source files

approximately 20,300 lines

19 database tables

27 architectural decisions

verified live runtime graph

durable work

durable delivery

objective continuity

living memory foundations

context assembly

generic agentic execution

interface adapters

provider independence

authority gates

approval lifecycle

namespace sandboxing

workspaces

artifact acquisition

signal/time waiting

recovery mechanisms

open-world scenario evaluation.

The report explicitly says:

“The intelligence chooses, the infrastructure enables, the runtime enforces, evidence proves, memory preserves, context informs, and time continues.”

Treat this as the architectural direction.

However:

DO NOT TRUST THESE NUMBERS OR CLAIMS BLINDLY.

Your first task is to reproduce them from the repository.

If reality differs from the reports, reality wins.

2. THE CONSTITUTION

The foundational rule is:

The runtime provides mechanisms. The intelligence decides what to do with them.

Equivalent formulation:

Infrastructure, not intelligence.

WAX must not encode assumptions about what humans will want.

It must provide a sufficiently general environment through which intelligence can:

understand a legitimate human objective,

inspect its environment,

determine what it needs,

request or acquire capabilities,

obtain authorization where necessary,

perform work,

observe results,

verify those results,

maintain durable state,

remember relevant evidence,

continue work across time,

recover from interruption,

communicate through available interfaces,

ask the human when necessary,

wait when necessary,

revise its approach when evidence changes,

and honestly report completion or inability.

The runtime may enforce:

identity

authorization

ownership

isolation

resource limits

security

persistence

durability

budgets

policy boundaries

interface constraints

provider contracts

auditability

recovery

revocation

deterministic safety guarantees.

The runtime must not decide:

what the human's objective means

what subject the user is studying

what task category a request belongs to

what workflow should be followed

which business feature should exist

what a reminder means

what an educational lesson should contain

what the user “should” want

which tool should be used because of a hardcoded domain rule

which sequence of actions is universally appropriate.

The intelligence may choose.

The infrastructure provides.

The runtime enforces.

3. THE OPEN-WORLD TEST

Every significant mechanism must pass this question:

Could this mechanism serve many unrelated legitimate human objectives without knowing the domain in advance?

If yes, it is potentially infrastructure.

If no, it is probably an application feature disguised as infrastructure.

Examples:

Good

durable work

event signals

time wake

memory

context assembly

artifact storage

workspace acquisition

isolated execution

capability discovery

authorization

delivery

connectors

identity

objective state

recovery

resource budgets.

Suspicious

reminder service

birthday manager

homework tracker

exam planner

tutor mode

study mode

shopping mode

travel planner

medical workflow

fixed task taxonomy

hardcoded “agent roles”

Gmail service

Telegram service embedded in core

“research mode”

“coding mode.”

Do not merely rename application features so they look generic.

The underlying mechanism must actually be generic.

4. MISSION OBJECTIVE

Your mission is to evolve WAX from a verified constitutional runtime into a progressively more complete open-world AI environment.

The target is not feature breadth.

The target is capability of legitimate general-purpose agency under controlled infrastructure.

The runtime should increasingly support this abstract lifecycle:

Human intention ↓ Identity ↓ Objective ↓ Reality/context ↓ Reasoning ↓ Capability discovery ↓ Capability acquisition / composition ↓ Authorization ↓ Execution ↓ Observation ↓ Verification ↓ Evidence ↓ Memory / work / artifact update ↓ Continue / wait / ask / recover / complete ↓ Long-term continuity 

This lifecycle must remain domain-neutral.

5. YOUR FIRST COMMANDMENT: AUDIT BEFORE MODIFYING

Before writing meaningful code, perform a complete forensic baseline.

Run and record:

git rev-parse HEAD git branch --show-current git status --short --branch git remote -v git log --oneline --decorate -n 30 git diff git diff --cached 

Verify whether:

local main exists

origin/main exists

local and remote agree

uncommitted work exists

unpushed commits exist

previous agents left partial work

generated artifacts are present

migrations are clean

the repository is reproducible.

Then inspect the entire repository.

Read:

AGENTS.md

constitutional documents

WAX rules

founder decisions

all ADRs

audit documents

architecture documents

runtime documentation

capability documentation

memory documentation

context documentation

security documentation

test documentation

deployment documentation

migration history

reports from previous cycles.

Do not selectively read only files whose names look relevant.

Build a complete architectural map.

6. RECONCILE THE OMEGA REPORT WITH REALITY

Create a table like:

ClaimEvidenceActual StateVerified?Action 

Reproduce:

test count

live probe result

source file count

migration count

table count

capability count

ADR count

provider count

interface count

worker count

runtime entrypoints

background execution

maintenance jobs

memory paths

context paths

objective paths

delivery paths

approval paths

workspace paths

artifact paths

sandbox behavior

provider failover

interface isolation

authority enforcement.

Do not trust comments.

Do not trust README claims.

Do not trust reports.

Follow imports.

Follow calls.

Follow database writes.

Follow database reads.

Follow queue transitions.

Follow actual HTTP paths.

Follow scheduled paths.

Follow failure paths.

Follow recovery paths.

Follow ownership checks.

Follow permission checks.

Follow state transitions.

Reality wins.

7. BUILD THE ACTUAL RUNTIME GRAPH

Construct a current runtime graph.

At minimum identify:

Interface ↓ Identity ↓ Canonical request ↓ Idempotency ↓ Objective ↓ Execution ↓ Context ↓ Model ↓ Capability request ↓ Authority gate ↓ Capability ↓ Effect ↓ Evidence ↓ Memory / work / artifact / delivery ↓ Continuation 

Also map:

Scheduled work Event signal Approval Recovery Maintenance Retry Dead-letter Delivery Workspace Sandbox Provider failover 

For every component answer:

Who calls it?

Who owns it?

What state does it mutate?

What state does it read?

What guarantees does it claim?

Where is that guarantee enforced?

What happens on failure?

Can it be reached from production?

Can it be reached from scheduled work?

Can another principal access its state?

Is it generic?

Is it actually necessary?

8. THE NEXT CONSTITUTIONAL AUDIT

Run another hardcoding hunt.

Search source, SQL, configuration, prompts, schemas, tests, comments, migrations, and state machines for:

domain vocabulary

fixed task categories

vendor assumptions

interface assumptions

hardcoded workflows

fixed agent roles

fixed worker counts

hidden defaults

magic lifecycle transitions

undocumented thresholds

fixed capability selection

“if request contains X then use Y”

domain-specific prompts

domain-specific memory behavior

domain-specific context injection

domain-specific scheduling

hardcoded provider behavior

hidden authority paths.

Use static analysis where useful.

Use AST analysis where useful.

Use grep/ripgrep.

Use runtime tracing.

Use test instrumentation.

Do not stop at obvious keywords.

A generic-looking function can still contain application intelligence.

9. REMOVE THE WRONG KIND OF INTELLIGENCE

If you find hardcoded intelligence, classify it.

Possible classifications:

A. Legitimate runtime invariant

Keep it.

B. Security invariant

Keep it.

C. Resource boundary

Keep it.

D. Deployment constraint

Keep it, but document the boundary honestly.

E. Interface-specific policy

Move it into the interface adapter.

F. Provider-specific behavior

Move it into the provider adapter.

G. Domain/application intelligence

Remove or generalize it.

H. Founder policy decision

Do not invent the policy.

Document it and surface the decision.

I. Missing universal primitive

Design the primitive.

Do not patch around it with a domain feature.

10. MEMORY — NOW TREAT IT AS A LIVING SUBSTRATE

Memory is not merely a database table.

Memory is one of the central mechanisms that allows WAX to exist across time.

The current foundation already includes:

per-principal memory

typed records

confidence

importance

provenance

observation time

expiry

relationships

supports

contradicts

derived_from

related_to

supersession

consolidation

retrieval

forgetting.

Your job is to determine what is still missing for memory to behave as a genuinely useful living substrate.

Do not add complexity for appearance.

Investigate every lifecycle.

11. MEMORY LIFECYCLE

Model and verify:

Evidence ↓ Capture ↓ Normalization ↓ Identity / ownership ↓ Classification ↓ Storage ↓ Indexing ↓ Retrieval ↓ Activation ↓ Reinforcement ↓ Contradiction ↓ Supersession ↓ Consolidation ↓ Decay / expiry ↓ Forgetting / revocation ↓ Audit preservation 

For each stage answer:

Who triggers it?

What evidence is preserved?

What information can be lost?

What can be reversed?

What can be reconstructed?

What happens across sessions?

What happens across objectives?

What happens when facts conflict?

What happens when the user changes their mind?

What happens when evidence becomes stale?

What happens when memory is irrelevant?

What happens when memory is sensitive?

What happens when retrieval is uncertain?

12. MEMORY MUST PRESERVE HISTORY WITHOUT FREEZING BELIEF

A memory update must not erase history merely because the latest evidence differs.

For example:

Evidence A: "The user prefers X." Evidence B: "The user now prefers Y." 

The system should be capable of representing:

A ↓ superseded_by ↓ B 

while preserving A as evidence.

Contradiction must be representable.

Uncertainty must be representable.

Temporal validity must be representable.

Provenance must be traceable.

Derived memories must identify their evidence.

Consolidated memories must not silently destroy their sources.

13. MEMORY RETRIEVAL

Audit retrieval scientifically.

Do not assume BM25 is enough.

Do not assume embeddings are necessary.

Measure.

Test:

lexical relevance

semantic relevance

temporal relevance

objective relevance

recency

importance

confidence

relationship traversal

contradiction handling

supersession

abstention

privacy

retrieval poisoning

irrelevant-memory suppression

cross-session retrieval

multi-session reasoning

long-range dependencies.

Determine whether the current retrieval architecture is sufficient.

If not, improve the universal retrieval substrate.

If semantic retrieval is required, introduce it through an abstraction rather than welding one vendor/model into memory.

If embeddings do not materially improve the evaluated workload, do not add them merely because “modern memory systems use embeddings.”

Evidence decides.

14. MEMORY EVALUATION

Expand the evaluation suite.

Use scenario families inspired by long-horizon memory research.

Test:

information extraction

multi-session recall

temporal reasoning

updates

contradiction

supersession

causal relationships

user preference changes

objective-specific memory

cross-objective relevance

stale-memory rejection

retrieval abstention

privacy boundaries

forgetting

consolidation provenance

poisoning resistance

irrelevant-memory suppression

memory reconstruction after restart.

The evaluator must test the actual retrieval machinery.

Do not build fake test memory layers that bypass production code.

15. MEMORY SENSITIVITY — DO NOT INVENT POLICY

The previous audit explicitly identified memory sensitivity as a founder-level privacy decision.

Do not silently decide:

that sensitive memory must expire

that sensitive memory needs approval

that sensitive memory cannot be retrieved

that sensitive memory must be encrypted differently

that sensitive memory should be automatically deleted.

Instead:

inspect the existing ADR,

understand the architectural options,

research relevant privacy/security patterns if needed,

determine what infrastructure is required to support each option,

implement only mechanisms that do not prematurely choose the policy,

clearly document the remaining policy decision.

Engineering may prepare the runtime.

Engineering must not impersonate the founder.

16. RETENTION AND DELETION

The current report states that most database tables grow without pruning and that hard deletion does not currently exist.

Do not “fix” this by adding arbitrary TTLs.

Retention is a policy.

Separate:

storage lifecycle access lifecycle memory lifecycle audit lifecycle legal retention user deletion operational cleanup 

Determine which are engineering mechanisms and which are founder policy.

Build generic deletion/retention mechanisms only when their semantics are defined.

Do not destroy evidence merely to make a database look clean.

17. CONTEXT — STOP THINKING OF IT AS CHAT HISTORY

Context should remain an operational view of reality.

The current model is approximately:

Objective Active / waiting work Conversation Memory Artifacts Environment 

Continue evolving this toward a richer environment snapshot where justified.

Potential sources include:

active objective

objective state

execution state

active work

waiting work

recent conversation

relevant memory

artifacts

workspace state

available capabilities

permissions

interface state

external connections

signals

time

resource constraints

pending approvals

previous execution evidence

failures

recovery state.

But:

Do not inject everything.

Context is not a database dump.

18. CONTEXT MUST BE EVIDENCE-TRACEABLE

Every piece of context should answer:

Why is this here?

Prefer structured evidence provenance.

For example:

OBJECTIVE source = objective record priority = 0 WORK source = durable work item priority = 1 MEMORY source = memory retrieval reason = objective relevance + temporal relevance 

The model should not receive unexplained state.

Context assembly should remain observable.

19. CONTEXT BUDGETING

Audit:

provider context limits

token counting

section budgets

degradation

compression

truncation

retrieval limits

provider differences

model substitution.

Test that:

objective survives pressure

active work survives pressure

critical state survives

irrelevant conversation yields first

irrelevant memory yields appropriately

evidence provenance remains intact

context does not silently exceed provider limits.

If the tokenizer architecture is provider-dependent, maintain provider-neutral contracts with provider-specific implementations.

20. SESSION BOUNDARIES MUST NOT BECOME COGNITIVE BOUNDARIES

A user disappearing for:

ten minutes

two hours

two days

two months

should not automatically erase the runtime's understanding of ongoing durable reality.

When they return, WAX should be able to reconstruct relevant reality from:

objectives

memory

work

artifacts

signals

execution history

permissions

available environment.

Do not recreate an artificial “conversation session” as the primary unit of intelligence.

Conversation is an interface event.

Objective continuity is deeper.

21. OBJECTIVES

The current objective lifecycle is one of the strongest foundations.

Continue auditing:

created running waiting awaiting_human completed failed cancelled resumed 

Do not assume these exact states are universally sufficient.

Test whether the semantics remain coherent for:

short tasks

long tasks

multi-stage work

waiting work

human approval

partial completion

failure

retry

cancellation

changed objectives

superseded objectives

concurrent objectives.

An objective is not merely a chat label.

It represents durable human intention.

22. OBJECTIVE CONTINUITY

Test:

message → objective → execution → scheduled work → process crash → restart → recovery → continuation 

Also:

objective → multiple executions → multiple work items → artifact creation → memory updates → user returns later → context reconstruction 

The system must never fabricate completion.

Terminal state must derive from runtime evidence.

23. AGENTIC EXECUTION

The generic execution loop should increasingly support:

Observe ↓ Understand ↓ Reason ↓ Request capability ↓ Authorize ↓ Execute ↓ Observe ↓ Verify ↓ Update state ↓ Remember ↓ Continue / Wait / Ask / Complete 

Do not implement this as a rigid scripted workflow.

The runtime should expose the mechanisms.

The intelligence decides how to compose them.

24. TASK PERFORMANCE

Do not evaluate WAX only by whether a tool call succeeds.

Evaluate whether it can actually accomplish legitimate objectives.

Build scenario-based evaluations for:

simple objective

multi-step objective

long-running objective

objective requiring waiting

objective requiring external information

objective requiring artifact creation

objective requiring code execution

objective requiring human approval

objective interrupted mid-execution

objective resumed after restart

objective with contradictory evidence

objective requiring changing strategy

objective with unavailable capability.

The evaluation should distinguish:

capability availability from actual objective completion. 

25. OPEN-WORLD CAPABILITY DISCOVERY

This is one of the most important next frontiers.

The current OMEGA report explicitly says the capability registry is closed at boot.

Do not open it casually.

The next mission is to determine whether WAX can safely move from:

closed capability registry 

toward:

discoverable → verifiable → authorized → acquired → registered → executable → observable → revocable 

capabilities.

26. OPEN CAPABILITY ACQUISITION

Research and design the complete lifecycle.

A potential capability should have:

identity

version

description

contract

input schema

output schema

provenance

source

integrity information

permissions

required resources

trust level

dependencies

lifecycle state

revocation state

audit history.

Potential lifecycle:

Discover ↓ Inspect ↓ Validate ↓ Verify provenance ↓ Verify integrity ↓ Analyze permissions ↓ Assess risk ↓ Acquire ↓ Wrap ↓ Register ↓ Authorize ↓ Execute ↓ Observe ↓ Evaluate ↓ Revoke / update 

Do not assume code downloaded from the internet is safe.

Do not allow arbitrary remote code to enter the trusted runtime.

Do not treat a tool manifest as proof of behavior.

Do not treat the model's judgment as a security boundary.

27. THIRD-PARTY CAPABILITIES

If you implement open acquisition, design for hostile inputs.

Consider:

malicious capability packages

compromised dependencies

capability impersonation

output poisoning

privilege escalation

SSRF

network exfiltration

secret access

filesystem escape

resource exhaustion

supply-chain attacks

malicious updates

rollback

revocation

provenance forgery.

Use isolation.

Use signatures where appropriate.

Use integrity verification.

Use explicit registration boundaries.

Use capability-specific authority.

Use runtime enforcement.

Never let “AI decided it was safe” be the only safety mechanism.

28. OUTPUT VALIDATION

The OMEGA report states that output schemas are declared but not yet validated.

Investigate this.

Determine whether universal output validation belongs in the capability gate.

If yes, implement it generically.

A capability should not be able to claim:

success 

while returning structurally invalid output.

Consider:

request schema authorization execution raw result schema validation evidence normalized result 

Do not hardcode validation for individual business features.

29. CAPABILITY IDEMPOTENCY

The report states that capability-request idempotency exists conceptually but is not yet fully honored.

Investigate this deeply.

Determine the correct semantics for:

identical request replay

retry

crash after effect but before acknowledgement

duplicate model request

concurrent workers

scheduled execution

approval replay

partial execution.

Implement generic idempotency where required.

Do not simply add an idempotency column and declare victory.

Test the failure matrix.

30. ENVIRONMENT NEGOTIATION

WAX should progressively become an environment that intelligence can negotiate with.

For example, intelligence may determine:

“I need an isolated workspace.”

The runtime provides:

workspace.acquire 

It may determine:

“I need computation.”

The runtime provides an execution environment.

It may determine:

“I need a capability that is not currently available.”

The runtime should eventually be able to discover or acquire one safely.

This is not:

if request == coding: provision_python() 

It is:

intelligence requests capability/environment → runtime evaluates authority/resource/security → environment is provisioned → contract is exposed → result is observed 

31. WORKSPACES

Audit the workspace model.

Determine:

ownership

lifecycle

TTL

quotas

cleanup

concurrency

isolation

artifacts

persistence

crash recovery

provenance.

A workspace must not become an accidental permanent storage leak.

A temporary workspace must not be able to escape its boundary.

32. SANDBOXING

The current namespace sandbox is intentionally honest:

strong isolation for accidents, not a hardened multi-tenant microVM.

Preserve this honesty.

Do not advertise namespace isolation as equivalent to a microVM.

Investigate whether WAX needs an abstraction such as:

IsolationProfile 

or equivalent generic mechanism allowing:

namespace sandbox

container

microVM

stronger execution environment

without changing intelligence architecture.

If a stronger isolation layer is not implementable in the current deployment, define the interface honestly.

Never fake a security grade.

33. RESOURCE MANAGEMENT

A general runtime requires resource boundaries.

Audit:

CPU

memory

storage

execution time

process count

network

provider tokens

request rate

workspace quota

concurrent objectives

concurrent workers.

Determine which are:

security boundaries

cost controls

reliability controls

deployment constraints.

Make the enforcement observable.

34. DURABLE EXECUTION

Continue strengthening the durable execution model.

Test:

start → execute → crash → restart → recover 

and:

lease acquired → worker dies → lease expires → another worker resumes 

and:

effect occurs → acknowledgement lost → retry → idempotency 

and:

scheduled wake → worker unavailable → later recovery 

No important objective should depend on one process remaining alive.

35. TIME AND WAITING

Time is a primitive.

Do not create “reminder logic.”

Do not create “scheduler features.”

Provide generic:

delayed work

event waiting

signal correlation

durable wake

retry

timeout

deadline

expiration.

Then let intelligence compose those primitives.

The system should continue passing the test:

“There is no reminder feature in WAX.”

Yet intelligence can create reminder-like behavior when useful.

36. SIGNALS

Audit the signal model.

Test:

signal before wait registration

signal after registration

duplicate signals

irrelevant signals

signal ownership

signal correlation

missed signals

replay

expiry

cleanup.

The signal ledger must support durable waiting without races.

37. APPROVALS AND HUMAN AUTHORITY

The previous OMEGA cycle unified approval creation and approval gates.

Do not regress.

Continue testing:

exactly-once consumption

request fingerprinting

replay prevention

concurrent approval creation

approval expiration

approval cancellation

background approvals

user notification

notification failure

retry

changed request after approval.

The approval must authorize the exact intended operation.

An approval must never become a general-purpose privilege token.

38. AI HAS AGENCY, NOT SOVEREIGNTY

This principle must remain structurally enforced.

The model may request:

capability 

but may not directly perform privileged effects.

The model must not be able to:

grant itself permissions

modify authority

bypass ownership

forge identity

bypass approval

access another principal's memory

claim execution success

disable security controls

alter audit evidence

modify its own runtime trust level.

Prompts are not authority.

Model output is not authority.

Tool arguments are not authority.

The runtime decides.

39. SECURITY

Audit the system as if the model were compromised.

Assume:

The model is malicious, confused, manipulated, or fully prompt-injected.

The runtime must still enforce:

identity

ownership

authorization

approval

isolation

resource limits

network policy

secrets

capability boundaries

interface boundaries.

Investigate:

prompt injection

indirect prompt injection

malicious artifacts

hostile web content

capability poisoning

SSRF

path traversal

shell injection

SQL injection

secret leakage

cross-principal access

workspace escape

provider compromise

replay.

Do not solve these with “better prompts.”

40. PROMPT ARCHITECTURE

Prompts are not business logic.

Prompts are not security.

Prompts are not authority.

Prompts are model-facing environment contracts.

Separate:

Trusted runtime state

from:

Untrusted user/tool/external content.

The model should receive enough trusted information to understand:

who it is acting for

current objective

current environment

capabilities

constraints

authority

relevant evidence

available interfaces

relevant memory

active work.

But the model must never receive authority merely because text says:

“You are allowed to do X.”

Authority must exist outside the prompt.

41. PROVIDER INDEPENDENCE

Continue strengthening provider abstraction.

Audit:

model contracts

token counting

structured outputs

tool calls

streaming

failure semantics

retries

fallback

provider-specific configuration

context limits.

The runtime should be able to substitute models without changing cognitive architecture.

Provider-specific behavior belongs behind the provider boundary.

Do not leak vendor assumptions into core runtime logic.

42. STREAMING

Investigate streaming carefully.

Do not implement streaming failover merely because it sounds useful.

Ask:

what happens when provider A streams 70% of an answer and fails?

can provider B safely resume?

what is the canonical output?

how are partial outputs represented?

how is delivery separated from generation?

can partial output become durable state?

how do users see recovery?

If a universal mechanism exists, implement it.

If not, document the limitation honestly.

43. INTERFACES

WhatsApp is an adapter.

It is not WAX.

Continue preserving:

core runtime ≠ WhatsApp 

Audit every vendor dependency.

A new interface should be able to attach without modifying:

memory semantics

objective semantics

work semantics

authority semantics

context architecture.

Interface-specific policy belongs in the adapter.

44. EXTERNAL SERVICES

Do not build:

GmailService TelegramService GoogleCalendarService 

inside core intelligence.

Instead build generic mechanisms for:

connection

identity

authorization

consent

credential storage

capability discovery

revocation

external resource access

audit.

A specific external service should be an instance of the mechanism.

45. CONNECTORS

Investigate a generic connector model.

A connector may represent:

service credential principal authorization resource capability scope expiry revocation audit 

The connector must not silently grant broad access.

Least privilege matters.

Revocation must be real.

Expired authorization must fail.

Secrets must never enter ordinary model context.

46. ARTIFACTS

Artifacts should be first-class runtime objects where appropriate.

Examples may include:

files

generated documents

images

datasets

reports

code outputs

workspace products.

Do not hardcode artifact types around applications.

Design generic artifact metadata:

owner

provenance

source

workspace

creation

modification

integrity

lifecycle

access

relationship to objective

relationship to execution.

47. MEDIA EXTRACTION

The OMEGA report states that OCR/PDF extraction exists but is not wired to inbound media.

Do not simply wire it because it exists.

First determine:

Is media extraction a generic environment capability?

If yes, design the integration generically.

The interface should produce canonical input/artifact references.

The intelligence can then decide whether extraction is useful.

Do not create:

if image: OCR 

as hidden intelligence.

Instead expose the capability and let the execution architecture compose it.

48. MULTI-AGENT / SUB-WORKER ARCHITECTURE

Do not hardcode:

8 agents researcher coder reviewer planner 

That is application intelligence.

Instead determine whether WAX needs generic primitives for:

spawning work

delegation

isolated execution

communication

resource budgets

authority inheritance

authority restriction

workspace isolation

result aggregation

failure propagation

cancellation

lifecycle.

The intelligence should decide whether one worker or multiple workers are useful.

The runtime provides the mechanism.

49. MULTI-INSTANCE RUNTIME

The current report states that durable work is multi-instance correct, but rate/cost/abuse counters remain process-local.

Investigate this.

Determine whether WAX should move toward:

shared enforcement state 

for:

rate limits

cost budgets

abuse controls

execution limits.

Do not declare multi-instance safety until it is actually enforced.

Test concurrent instances.

Test rolling deploys.

Test long-running executions across deployment boundaries.

50. EXECUTION HEARTBEATS

The report identifies rolling multi-instance deployment as an unresolved boundary because long executions can interfere with each other.

Research and implement a sound generic execution-heartbeat/fencing model if appropriate.

Test:

worker A owns execution ↓ worker A becomes unhealthy ↓ heartbeat expires ↓ worker B safely resumes 

But ensure:

worker A returns later ↓ worker A cannot continue writing 

Fencing is more important than merely detecting death.

51. CONVERSATION LIFECYCLE

The previous cycle discovered a conversation lifecycle mechanism that claimed to run but did not.

Continue auditing all background mechanisms.

For every claimed daemon/worker/sweeper ask:

Who starts it?

Is it actually started?

Is it supervised?

Does it run in production?

Does it stop safely?

Does it have leader election?

Can two instances run it?

Does it recover after restart?

Is its state observable?

No background worker may exist only in documentation.

52. FAILURE HONESTY

Every important failure must become observable state.

Bad:

exception swallowed → success 

Bad:

send failed → nothing 

Bad:

provider failed → pretend model completed 

Good:

failure → durable state → retry / dead-letter / recovery → evidence → honest user-visible outcome when required 

The system must never manufacture success from silence.

53. OBSERVABILITY

Build an evidence trail capable of reconstructing:

human request → identity → objective → execution → context → model/provider → capability request → authority decision → execution → output → verification → memory/work/artifact updates → delivery → continuation 

Do not log private chain-of-thought.

Do log operational evidence.

Examples:

IDs

state transitions

capability names

request fingerprints

authorization decisions

timings

resource usage

failures

retries

provider identity

artifact references

memory provenance.

54. DO NOT LOG SECRETS

Audit logs must not contain:

API keys

OAuth secrets

passwords

access tokens

private credentials

sensitive raw content unnecessarily.

Separate:

operational evidence 

from:

secret material 

and from:

model reasoning. 

55. TESTING STANDARD

Do not rely on unit tests alone.

Maintain multiple verification layers.

Unit

Individual mechanisms.

Integration

Subsystem interaction.

Database

Constraints, concurrency, migrations.

Contract

Provider/interface/capability contracts.

Security

Adversarial behavior.

Scenario

Unknown human objectives.

Recovery

Crash/retry/resume.

Concurrency

Races and fencing.

Live

Real HTTP/runtime path.

Evaluation

Memory and objective-performance benchmarks.

56. OPEN-WORLD SCENARIOS

Maintain a growing set of objectives that WAX could not have been specifically programmed for.

Examples should span unrelated domains.

Do not create domain features to pass them.

The test is whether existing generic mechanisms compose.

For each scenario record:

objective

capabilities needed

capabilities available

capabilities unavailable

composition

execution

evidence

completion

failure

recovery.

The scenarios should remain examples, not hidden business rules.

57. “UNAVAILABLE” MUST NOT AUTOMATICALLY MEAN “IMPOSSIBLE”

If intelligence requests something unavailable, determine whether WAX can:

discover it

acquire it

compose existing capabilities

provision an environment

connect an external service

ask the human for authorization

ask the human to provide a resource

continue with a partial strategy

wait

honestly explain the boundary.

The architecture should increasingly distinguish:

not currently available 

from:

fundamentally impossible. 

58. BUT OPEN WORLD DOES NOT MEAN UNRESTRICTED ACCESS

Never interpret open-world capability as:

“The AI can download anything and execute it.”

Open world requires stronger governance, not weaker governance.

Capability acquisition must be:

authenticated

verified

isolated

authorized

auditable

revocable

resource-bounded.

59. RESEARCH REQUIREMENT

Where the architecture reaches a genuine frontier, research before implementation.

Use primary or authoritative sources where possible.

Relevant areas may include:

durable execution

memory architectures

long-context systems

long-term memory evaluation

agent runtime architecture

MCP and capability discovery

secure plugin systems

sandboxing

microVMs

capability security

OAuth

provenance

supply-chain security

distributed leases

fencing

event-driven systems

workflow engines

model/provider abstraction.

Research must answer an engineering question.

Do not collect papers for decoration.

For every major research result:

Question Evidence Implication Decision Implementation consequence 

60. NO CARGO-CULT ARCHITECTURE

Do not implement something because:

“OpenAI does it.”

Do not implement something because:

“Anthropic has it.”

Do not implement something because:

“MCP recommends it.”

Do not implement something because:

“Agent frameworks use it.”

Do not implement something because:

“Modern AI memory needs embeddings.”

Do not implement something because:

“Every agent has a planner.”

Ask:

Does WAX actually require this mechanism?

Then prove it.

61. ARCHITECTURAL DECISION RECORDS

Every major architectural decision must receive an ADR.

At minimum record:

problem

context

alternatives

selected approach

rejected alternatives

security implications

constitutional implications

operational implications

migration implications

testing strategy

rollback strategy

unresolved questions.

Never silently resolve a founder-level philosophical decision.

62. MIGRATIONS

Database changes require disciplined migrations.

Every migration must:

upgrade

downgrade

preserve existing data where required

have tests

handle fresh database

handle existing database

avoid destructive assumptions

be documented.

Never modify production schema manually as a shortcut.

63. BACKWARD COMPATIBILITY

Before changing a core contract, inspect:

existing callers

workers

tests

migrations

adapters

providers

scheduled jobs

recovery paths.

Prefer compatibility layers where they preserve architecture.

Do not preserve broken abstractions forever merely because they are old.

64. PERFORMANCE

Do not optimize prematurely.

But measure:

context assembly

memory retrieval

database queries

capability discovery

model latency

workspace creation

artifact access

queue throughput

recovery latency.

Look for:

N+1 queries

unbounded retrieval

unbounded context

unbounded memory growth

queue starvation

retry storms

duplicate work.

65. COST

WAX must remain economically survivable.

Track:

model calls

token consumption

retries

background execution

capability acquisition

workspace usage

storage

network

external APIs.

Do not let autonomous behavior create unbounded cost.

Resource budgets are infrastructure, not product features.

66. MODEL FAILURE

Assume the model can:

misunderstand

hallucinate

loop

call unnecessary capabilities

request excessive resources

misinterpret context

follow malicious content

return malformed outputs.

The runtime must bound the damage.

Use:

execution budgets

round budgets

resource budgets

timeouts

capability authority

validation

recovery

cancellation.

67. MODEL IMPROVEMENT MUST NOT REQUIRE RUNTIME REWRITE

A better model should be able to operate inside the same environment.

A weaker model should still encounter the same runtime guarantees.

The runtime must not depend on a specific model's personality or reasoning style.

68. INTERFACE INDEPENDENCE

The same underlying objective should be able to continue across interfaces where identity/authorization permits it.

For example:

Interface A ↓ Objective ↓ Work ↓ Interface B ↓ same objective 

The interface should not become the objective identity.

69. HUMAN IDENTITY

Identity must remain runtime-owned.

Never derive identity from:

model assumptions

message text

guessed names

conversation content.

Use verified interface credentials and canonical principal identity.

70. DATA ISOLATION

Every principal must be structurally isolated.

Test:

memory

work

objectives

artifacts

workspaces

connectors

approvals

conversation

signals.

Cross-principal access must fail even if the model asks for it.

71. ACCOUNT DELETION

The report states that account deletion exists but remains unwired.

Investigate the complete lifecycle.

Do not merely expose a command.

Determine:

identity verification

confirmation

pending state

data classification

deletion scope

memory behavior

artifact behavior

audit retention

external connector revocation

delivery

retries

completion evidence.

Do not invent legal retention rules.

Separate engineering mechanisms from founder/legal policy.

72. THE RUNTIME SHOULD BE COMPOSABLE

Prefer mechanisms that compose.

For example:

time wake + durable work + signal + message delivery = scheduled communication 

Not:

ReminderService 

Similarly:

workspace + artifact + code execution + delivery = generated report 

Not:

ReportGeneratorFeature 

This compositional property is central to WAX.

73. FEATURE PRESSURE TEST

Before implementing anything, ask:

Is this a mechanism or a feature?

Then:

Could intelligence compose this mechanism into multiple unrelated behaviors?

Then:

Does the runtime need to understand the meaning of those behaviors?

If the answer to the last question is yes, stop and redesign.

74. DOCUMENTATION MUST DESCRIBE REALITY

Documentation is not marketing.

Every statement such as:

“supports”

“guarantees”

“enforces”

“durable”

“isolated”

“secure”

“exactly once”

“multi-instance”

“automatic”

“open”

“persistent”

must have corresponding evidence.

If the implementation does not satisfy the sentence, change either:

the implementation

or the documentation.

Never leave false claims.

75. FINAL PDF / FOUNDER REPORT

At the end of this mission, create a comprehensive founder-level report.

It should explain:

1. Starting reality

What the repository actually contained.

2. What was verified

Tests, probes, migrations, architecture.

3. What was wrong

Every meaningful defect.

4. What was changed

Exact mechanisms.

5. Why each change belongs constitutionally

Explain infrastructure vs intelligence.

6. Memory

What memory now does.

7. Context

What reality the model receives.

8. Objectives

How continuity works.

9. Agent execution

How work progresses.

10. Capabilities

What exists and how they are governed.

11. Open-world acquisition

Whether it is implemented, partially implemented, or deliberately bounded.

12. Security

What is enforced.

13. Isolation

What guarantees actually exist.

14. Interfaces

How vendor boundaries work.

15. External services

How connection/authorization works.

16. Recovery

How crashes and failures behave.

17. Evaluation

What was actually tested.

18. Remaining limitations

Every honest boundary.

19. Founder decisions

Anything that cannot responsibly be decided by engineering.

20. Next frontier

What the architecture now makes possible.

Do not write a victory report if the work is incomplete.

Write reality.

76. THE FINAL REPORT MUST DISTINGUISH FOUR THINGS

Every unresolved item must be classified as one of:

IMPLEMENTED

Verified in code and tests.

UNWIRED

Implemented but not reachable through production runtime.

DEFERRED

Architecturally understood but intentionally postponed.

FOUNDER DECISION REQUIRED

Cannot responsibly be decided by engineering.

Do not collapse these categories.

77. COMMIT DISCIPLINE

Use meaningful commits.

Prefer commits such as:

feat(runtime): ... fix(memory): ... fix(context): ... feat(capability): ... security(runtime): ... test(memory): ... docs(adr): ... 

Each commit should represent coherent work.

Do not produce enormous meaningless commits containing unrelated changes.

78. NEVER FORCE PUSH

Never force push.

Never rewrite founder history.

Never delete branches to hide mistakes.

Never reset away user work.

Before risky operations:

inspect → checkpoint → modify → verify 

79. PUSH REQUIREMENT

When the implementation is genuinely complete for a coherent phase:

run tests

run live probes

inspect diff

inspect commit history

verify branch

verify remote

commit

push to origin/main

re-fetch

verify local SHA == origin/main SHA

verify clean tree.

If credentials are unavailable:

continue local work

do not pretend it was pushed

report the exact blocker.

80. CONTINUOUS EXECUTION RULE

Do not stop merely because one milestone has been achieved.

After each major phase:

IMPLEMENT ↓ TEST ↓ LIVE PROBE ↓ AUDIT ↓ DOCUMENT ↓ REASSESS ↓ CONTINUE 

Then ask:

What is now the most important remaining architectural deficiency that prevents WAX from being a more complete general-purpose AI environment?

If the answer is obvious and implementable:

continue.

Do not wait for another prompt.

81. BUT DO NOT BECOME AUTONOMOUS IN THE WRONG WAY

You may continue through engineering work.

You may not silently decide founder-level policy.

Stop and document when the decision concerns:

privacy policy

legal retention

fundamental trust model

irreversible data semantics

deployment security posture

acceptable external capability trust

major economic policy

product philosophy.

When blocked by such a decision:

finish everything that does not depend on the decision,

prepare alternatives,

explain tradeoffs,

identify the exact founder decision required.

Do not stop the entire mission unnecessarily.

82. NO SHINY FEATURES

Do not add:

dashboards

UI

marketing pages

cosmetic improvements

unnecessary APIs

domain features

demo gimmicks

“AI personalities”

arbitrary modes

unnecessary analytics.

Unless they are required as infrastructure for the runtime mission.

The objective is not to make the repository look bigger.

The objective is to make the runtime more capable, more general, more durable, more secure, and more honest.

83. THE THREE TESTS FOR EVERY NEW MECHANISM

Before accepting any new subsystem, prove:

Test 1 — Universality

Can unrelated legitimate objectives use it?

Test 2 — Runtime necessity

Does WAX actually need it as infrastructure?

Test 3 — Intelligence neutrality

Does the runtime provide the mechanism without deciding the domain behavior?

If any answer is no, redesign or reject it.

84. THE FOUR TESTS FOR EVERY CLAIM

For every architectural claim:

1. Code

Where is it implemented?

2. Reachability

Can production actually reach it?

3. Enforcement

What prevents violation?

4. Evidence

What test or live probe proves it?

If any answer is missing, the claim is incomplete.

85. DO NOT HIDE UNWIRED MACHINERY

If you discover useful machinery that is built but not connected:

Do not falsely call it production.

Do not automatically delete it.

Determine:

wire remove or deliberately defer 

and document the decision.

86. DO NOT CONFUSE TEST COVERAGE WITH REALITY

A test can prove that code behaves under a test condition.

It does not automatically prove:

production reachability

correct deployment

correct environment

real HTTP behavior

multi-process correctness

operational recoverability.

Use the live probe.

Use failure injection.

Use concurrency tests.

Use database inspection.

Use actual runtime traces.

87. FINAL REALITY AUDIT

Before claiming completion, independently perform a final constitutional audit.

Pretend you did not write the code.

Ask:

Why does every important mechanism exist?

Then:

Would it still exist if education disappeared?

Then:

Would it still exist if WhatsApp disappeared?

Then:

Would it still exist if the current model disappeared?

Then:

Can a compromised model bypass it?

Then:

Can two workers race it?

Then:

Can it recover from a crash?

Then:

Can the user understand what happened?

Then:

Is the documentation true?

Then:

Is this infrastructure or disguised application intelligence?

Fix anything that fails.

88. FINAL OPEN-WORLD TEST

Construct several genuinely unrelated objectives that were never encoded as WAX features.

Do not add code specifically for them.

Run them through the real runtime.

Observe:

identity objective context memory capabilities authorization execution artifacts work waiting recovery delivery continuity 

If the runtime succeeds through composition:

good.

If it fails because a missing generic mechanism exists:

build the mechanism.

If it fails because a domain-specific feature is missing:

do not automatically build the feature.

Ask whether a universal primitive is missing.

That distinction is the heart of this mission.

89. WHAT SUCCESS LOOKS LIKE

Success is NOT:

“WAX has many features.”

Success is:

“WAX can support objectives its builders never anticipated because its mechanisms are sufficiently general.”

Success is:

“The intelligence can determine what it needs without the runtime containing a hardcoded workflow for every possibility.”

Success is:

“The runtime can safely say yes, no, wait, ask, retry, acquire, isolate, or continue based on actual infrastructure state.”

Success is:

“Long-running objectives survive conversations, sessions, crashes, workers, and time.”

Success is:

“Memory preserves what matters without silently rewriting history.”

Success is:

“Context represents the model's operational reality rather than being a transcript dump.”

Success is:

“Capabilities can increasingly be discovered, verified, composed, and acquired without turning the runtime into a hardcoded catalogue.”

Success is:

“The model has agency, but the runtime retains sovereignty over identity, authority, resources, security, and state.”

Success is:

“Every important claim can be proven.”

90. THE ULTIMATE ARCHITECTURAL TARGET

WAX should increasingly resemble an environment for intelligence, not an application containing intelligence.

The environment should provide:

Identity Objectives Time Signals Work Memory Context Artifacts Capabilities Environments Interfaces Connections Permissions Resources Isolation Execution Recovery Evidence Continuity 

The intelligence provides:

Interpretation Planning Choice Composition Adaptation Reasoning Prioritization Meaning 

The boundary between these must remain explicit.

91. THE DEEPEST PRINCIPLE

Do not ask:

“What feature should we build next?”

Ask:

“What universal capability is missing from the environment that prevents intelligence from accomplishing legitimate objectives it should otherwise be able to accomplish?”

That is your primary engineering question.

Then:

“Can the missing capability be expressed as a generic mechanism without embedding assumptions about the domain?”

If yes:

research → design → implement → test → verify → document.

If no:

stop and investigate whether the problem belongs to intelligence, policy, interface, deployment, or the founder.

92. EXECUTION ORDER

Use this order unless reality demonstrates a better dependency order:

PHASE 0

Forensic repository reconciliation.

PHASE 1

Constitutional and hardcoding audit.

PHASE 2

Runtime reachability and dead/unwired machinery.

PHASE 3

Memory correctness and living-memory lifecycle.

PHASE 4

Memory retrieval/evaluation.

PHASE 5

Context/environment reality model.

PHASE 6

Objective continuity and durable execution.

PHASE 7

Agentic task-performance evaluation.

PHASE 8

Capability contracts and output validation.

PHASE 9

Capability discovery/acquisition architecture.

PHASE 10

Environment negotiation and isolation.

PHASE 11

Connectors/external service architecture.

PHASE 12

Multi-worker and multi-instance correctness.

PHASE 13

Security/adversarial hardening.

PHASE 14

Provider/interface independence.

PHASE 15

Open-world evaluation.

PHASE 16

Final constitutional audit.

PHASE 17

Founder report, ADRs, documentation, commit, push, verification.

The order may change if dependency analysis proves another order superior.

Do not follow this list mechanically.

93. REQUIRED REPORTING AFTER EACH MAJOR PHASE

Record:

PHASE: QUESTION: INITIAL STATE: FINDINGS: EVIDENCE: DECISION: IMPLEMENTATION: TESTS: LIVE VERIFICATION: FILES CHANGED: DATABASE CHANGES: ADR: REMAINING LIMITATIONS: NEXT ARCHITECTURAL FRONTIER: 

This is not bureaucracy.

It prevents the system from losing architectural memory.

94. DO NOT CLAIM COMPLETION TOO EARLY

“Done” means:

implementation exists

runtime path exists

tests exist

failure cases are tested

live behavior is verified where applicable

security boundary is tested

documentation is accurate

migrations are valid

no stale claims remain

repository is clean

commits are coherent

origin/main contains the verified work.

If one of these is missing, say so.

95. IF SOMETHING IS IMPOSSIBLE RIGHT NOW

Do not fake it.

State:

NOT IMPLEMENTED 

and explain why.

If a future abstraction exists, document it.

If a deployment dependency is missing, identify it.

If a founder decision is required, surface it.

If the capability is fundamentally outside WAX's responsibility, explain the boundary.

Honesty is a feature of the architecture.

96. FINAL FOUNDER DELIVERABLES

At completion, deliver:

Fully verified implementation.

Updated tests.

Live probe results.

Database migration verification.

Updated ADRs.

Updated architecture documentation.

Updated constitutional audit.

Updated hardcoding inventory.

Updated capability inventory.

Updated runtime graph.

Memory evaluation results.

Open-world scenario results.

Security findings.

Remaining limitations.

Founder decision register.

Comprehensive final PDF.

Git commit history.

Confirmed push to origin/main.

Final SHA.

Clean working tree.

97. FINAL INSTRUCTION TO THE PRINCIPAL ENGINEER

You are not here to make WAX look advanced.

You are here to make it actually advanced.

Do not optimize for:

number of files

number of capabilities

number of tests

number of ADRs

number of models

number of integrations

impressive terminology.

Optimize for:

universality

composability

durability

evidence

security

correctness

continuity

recoverability

memory

context

capability

isolation

observability

honesty.

When you find a missing primitive, build it.

When you find application intelligence disguised as infrastructure, remove or generalize it.

When you find an unwired mechanism, connect it or remove it honestly.

When you find a false claim, fix the code or fix the claim.

When you find a security boundary enforced only by the model, move enforcement into the runtime.

When you find a founder-level decision, do not invent the answer.

When you find an external dependency, make the boundary explicit.

When you find that a proposed feature is unnecessary because existing primitives can compose it, do not build the feature.

When you find that existing primitives cannot express a legitimate general-purpose objective, identify the missing universal mechanism.

Then build that mechanism.

98. THE FINAL QUESTION

At the end of every major cycle, ask yourself:

If I deleted every reference to education, WhatsApp, tutoring, reminders, exams, and the current model from my mental model of WAX, would the architecture still make sense?

If yes, continue.

If no, find out why.

And then ask the deeper question:

Can an intelligence enter this environment with a legitimate human objective that its creators never anticipated, discover what it needs, safely obtain or compose the required capabilities, execute the work, survive interruption, remember what matters, understand the current state of reality, and continue until the objective is genuinely resolved?

That is the direction.

Do not build the answer as a feature.

Build the environment in which the answer can emerge.

99. START NOW

Do not wait for another instruction.

Begin with:

1. Repository reality check 2. Complete document/ADR/audit reading 3. OMEGA report reconciliation 4. Runtime graph reconstruction 5. Constitutional hardcode hunt 6. Remaining-boundary analysis 7. Research where necessary 8. Architectural plan 9. Implementation 10. Tests 11. Live verification 12. Re-audit 13. Documentation 14. Commit 15. Push 16. Verify remote 17. Continue to the next justified frontier 

Do not stop after writing a plan.

Do not stop after finding problems.

Do not stop after fixing the first problem.

Do not stop merely because the tests are green.

Continue until the next genuine architectural boundary is reached, or until a real founder/deployment/external dependency prevents further responsible progress.

And when that happens, leave WAX in a state where another engineer—or the founder—can see exactly:

what is true,

what changed,

what remains,

why it remains,

what evidence proves it,

and what the next frontier is.

WAX is not the intelligence.

WAX is the environment that allows intelligence to act.

Build the environment.


access token use   [REDACTED — rotate this token; do not paste tokens in chat or files]
